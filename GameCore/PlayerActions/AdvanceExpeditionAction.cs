using GameCore.Interfaces;

namespace GameCore.PlayerActions
{
    /// <summary>
    /// The Demon Lord stops holding the party at the stairs. The heroes then pick a room on
    /// the floor they stand on and break into it, which starts the encounter.
    /// </summary>
    public class AdvanceExpeditionAction : IPlayerAction { }
}
